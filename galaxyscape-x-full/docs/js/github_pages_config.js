/**
 * GitHub Pages Runtime Configuration
 * ----------------------------------
 * When deploying the static UI to GitHub Pages (or any CDN),
 * set `window.GALAXYSCAPE_API_BASE` to the public URL of your
 * running Flask backend (e.g. https://your-domain.com).
 *
 * Example:
 *   window.GALAXYSCAPE_API_BASE = 'https://polaris-capital-api.onrender.com';
 */

window.GALAXYSCAPE_API_BASE = window.GALAXYSCAPE_API_BASE || '';

